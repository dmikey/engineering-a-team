# ──────────────────────────────────────────────────────────────────────────────
# Council Discussion — Casey (Council Moderator)
#
# Convenes a multi-agent council where QA Engineer, Project Manager, and
# Product Owner each analyse a topic independently. The Council Moderator
# then synthesises all perspectives into a consensus decision and action list.
# Results are posted to GitHub Discussions (or Issues as fallback).
#
# Triggers:  issues or PRs labeled "council-review"
#            discussion comments
#            Product Owner workflow completion (success)
#            schedule (weekday product decision sweep)
#            workflow_dispatch
#            workflow_call (from agent-router or other workflows)
#
# Model:     gpt-5.4 (overridable via COUNCIL_MODEL variable)
# ──────────────────────────────────────────────────────────────────────────────
name: Council Discussion

on:
  schedule:
    # Every weekday at 14:30 UTC, shortly after Product Owner runs.
    - cron: '30 14 * * 1-5'
  issues:
    types: [labeled]
  pull_request:
    types: [labeled]
  discussion_comment:
    types: [created]
  workflow_run:
    workflows: ["Product Owner Agent", "Task Assignment"]
    types: [completed]
  workflow_dispatch:
    inputs:
      mode:
        description: 'discussion | role-adjustment'
        required: false
        default: discussion
      topic:
        description: 'Topic for council discussion'
        required: true
      context:
        description: 'Additional context or background'
        required: false
      issue_number:
        description: 'Related issue or PR number (optional)'
        required: false
      discussion_id:
        description: 'Discussion node ID to reply into (optional backfill)'
        required: false
      discussion_reply_to_id:
        description: 'Discussion comment node ID to reply to (optional backfill)'
        required: false
  workflow_call:
    inputs:
      mode:
        type: string
        description: 'discussion | role-adjustment'
        required: false
        default: discussion
      topic:
        type: string
        description: 'Topic for council discussion'
        required: true
      context:
        type: string
        description: 'Additional context or background'
        required: false
      issue_number:
        type: string
        description: 'Related issue or PR number'
        required: false
      discussion_id:
        type: string
        description: 'Discussion node ID to reply into'
        required: false
      discussion_reply_to_id:
        type: string
        description: 'Discussion comment node ID to reply to'
        required: false
    outputs:
      decision_url:
        description: 'URL of the posted council decision'
        value: ${{ jobs.council.outputs.decision_url }}
      decision:
        description: 'The council decision text'
        value: ${{ jobs.council.outputs.decision }}
    secrets:
      COPILOT_GITHUB_TOKEN:
        required: false

permissions:
  contents: read
  issues: write
  copilot-requests: write
  pull-requests: write
  discussions: write

env:
  COUNCIL_MODEL: ${{ vars.COUNCIL_MODEL || 'gpt-5.4' }}
  COUNCIL_ROLE_ADJUSTMENT_MIN_SCORE: ${{ vars.COUNCIL_ROLE_ADJUSTMENT_MIN_SCORE || '80' }}
  AGENT_MODEL: ${{ vars.AGENT_MODEL || 'gpt-5-mini' }}
  AGENT_MAX_TOKENS: ${{ vars.AGENT_MAX_TOKENS || '2048' }}
  AGENT_TEMPERATURE: ${{ vars.AGENT_TEMPERATURE || '0.7' }}
  QA_AGENT_SKILLS: ${{ vars.QA_AGENT_SKILLS || 'code-review,issue-creation,pr-feedback,security-scan' }}
  PM_AGENT_SKILLS: ${{ vars.PM_AGENT_SKILLS || 'backlog-grooming,milestone-management,discussion-creation,issue-labeling,skill-development-analysis' }}
  PO_AGENT_SKILLS: ${{ vars.PO_AGENT_SKILLS || 'feature-suggestion,playwright-testing,issue-creation,discussion-facilitation,product-analysis' }}
  COUNCIL_AGENT_SKILLS: ${{ vars.COUNCIL_AGENT_SKILLS || 'discussion-creation' }}

jobs:
  council:
    name: Council Discussion
    runs-on: ubuntu-latest
    # Run on explicit requests plus automated product-decision reviews.
    if: |
      github.event_name == 'schedule' ||
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'workflow_call' ||
      (github.event_name == 'workflow_run' &&
        github.event.workflow_run.conclusion == 'success' &&
        github.event.workflow_run.event != 'discussion') ||
      (github.event_name == 'issues' &&
       contains(github.event.issue.labels.*.name, 'council-review')) ||
      (github.event_name == 'pull_request' &&
        contains(github.event.pull_request.labels.*.name, 'council-review')) ||
            (github.event_name == 'discussion_comment' &&
        github.event.action == 'created' &&
        github.actor != 'github-actions[bot]')

    outputs:
      decision_url: ${{ steps.post-decision.outputs.url }}
      decision: ${{ steps.final-record.outputs.body }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # ── Determine topic and context ──────────────────────────────────────
      - name: Resolve topic and context
        id: resolve
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          EVENT_NAME: ${{ github.event_name }}
          INPUT_MODE: ${{ inputs.mode }}
          INPUT_TOPIC: ${{ inputs.topic }}
          INPUT_CONTEXT: ${{ inputs.context }}
          INPUT_ISSUE_NUMBER: ${{ inputs.issue_number }}
          INPUT_DISCUSSION_ID: ${{ inputs.discussion_id }}
          INPUT_DISCUSSION_REPLY_TO_ID: ${{ inputs.discussion_reply_to_id }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
          ISSUE_BODY: ${{ github.event.issue.body }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_BODY: ${{ github.event.pull_request.body }}
          DISCUSSION_NUMBER: ${{ github.event.discussion.number }}
          DISCUSSION_TITLE: ${{ github.event.discussion.title }}
          DISCUSSION_BODY: ${{ github.event.discussion.body }}
          DISCUSSION_URL: ${{ github.event.discussion.html_url }}
          DISCUSSION_NODE_ID: ${{ github.event.discussion.node_id }}
          DISCUSSION_COMMENT_BODY: ${{ github.event.comment.body }}
          DISCUSSION_COMMENT_URL: ${{ github.event.comment.html_url }}
          DISCUSSION_COMMENT_NODE_ID: ${{ github.event.comment.node_id }}
          DISCUSSION_COMMENT_AUTHOR: ${{ github.event.comment.user.login }}
        run: |
          # Determine topic
          MODE="$INPUT_MODE"
          if [ -z "$MODE" ]; then
            if [ "$EVENT_NAME" = "workflow_run" ] && [ "${{ github.event.workflow_run.name }}" = "Task Assignment" ]; then
             MODE="role-adjustment"
            else
             MODE="discussion"
            fi
          fi
          DISCUSSION_REPLY_ID=""
          DISCUSSION_REPLY_TO_ID=""

          if [ -n "$INPUT_TOPIC" ]; then
            TOPIC="$INPUT_TOPIC"
            CONTEXT="$INPUT_CONTEXT"
            REF_NUMBER="$INPUT_ISSUE_NUMBER"
            DISCUSSION_REPLY_ID="$INPUT_DISCUSSION_ID"
            DISCUSSION_REPLY_TO_ID="$INPUT_DISCUSSION_REPLY_TO_ID"
          elif [ "$EVENT_NAME" = "workflow_run" ]; then
            if [ "$MODE" = "role-adjustment" ]; then
             TOPIC="Dynamic Agent Role Adjustment — Task Assignment run #${{ github.event.workflow_run.run_number }}"
             CONTEXT="Triggered from Task Assignment workflow completion (${{ github.event.workflow_run.html_url }})"
            else
             TOPIC="Product Decision Review — Product Owner run #${{ github.event.workflow_run.run_number }}"
             CONTEXT="Triggered from Product Owner workflow completion (${{ github.event.workflow_run.html_url }})"
            fi
            REF_NUMBER=""
          elif [ "$EVENT_NAME" = "schedule" ]; then
            TOPIC="Daily Product Decision Review"
            CONTEXT="Scheduled council sweep of recent product-owner decisions and feature proposals."
            REF_NUMBER=""
          elif [ "$EVENT_NAME" = "issues" ]; then
            TOPIC="Issue #$ISSUE_NUMBER: $ISSUE_TITLE"
            CONTEXT="$ISSUE_BODY"
            REF_NUMBER="$ISSUE_NUMBER"
          elif [ "$EVENT_NAME" = "pull_request" ]; then
            TOPIC="PR #$PR_NUMBER: $PR_TITLE"
            CONTEXT="$PR_BODY"
            REF_NUMBER="$PR_NUMBER"
          elif [ "$EVENT_NAME" = "discussion_comment" ]; then
            TOPIC="Discussion #$DISCUSSION_NUMBER: $DISCUSSION_TITLE"
            CONTEXT=$(printf '%s\n\nDiscussion URL: %s\nComment URL: %s\nComment author: %s\n\nComment body:\n%s\n\nOriginal discussion body:\n%s' \
              "A user requested council input by commenting on a GitHub Discussion." \
              "$DISCUSSION_URL" \
              "$DISCUSSION_COMMENT_URL" \
              "$DISCUSSION_COMMENT_AUTHOR" \
              "$DISCUSSION_COMMENT_BODY" \
              "$DISCUSSION_BODY"
            )
            REF_NUMBER=""
            DISCUSSION_REPLY_ID="$DISCUSSION_NODE_ID"
            DISCUSSION_REPLY_TO_ID="$DISCUSSION_COMMENT_NODE_ID"
          else
            TOPIC="Council Review — ${{ github.run_id }}"
            CONTEXT=""
            REF_NUMBER=""
          fi

          # Gather repository context
          README=$(cat README.md 2>/dev/null | head -50 || echo "")
          OPEN_ISSUES_COUNT=$(gh issue list --state open --limit 1 --json number --jq '. | length' 2>/dev/null || echo "0")
          PRODUCT_DECISIONS=$(gh issue list \
            --state open \
            --label "product-owner" \
            --limit 10 \
            --json number,title,createdAt,labels \
            2>/dev/null || echo "[]")
          RECENT_COMMITS=$(git log --oneline --no-merges -10 2>/dev/null || echo "")
          CURRENT_DATE=$(date -u +"%Y-%m-%dT%H:%M:%SZ")

          {
            echo "mode=$MODE"
            echo "topic=$TOPIC"
            echo "ref_number=$REF_NUMBER"
            echo "discussion_reply_id=$DISCUSSION_REPLY_ID"
            echo "discussion_reply_to_id=$DISCUSSION_REPLY_TO_ID"
            echo "current_date=$CURRENT_DATE"
            echo "base_context<<COUNCIL_CTX_EOF"
            echo "## Topic"
            echo "$TOPIC"
            echo ""
            echo "## Context"
            echo "$CONTEXT"
            echo ""
            echo "## Repository Background"
            echo "Repository: ${{ github.repository }}"
            echo "Date: $CURRENT_DATE"
            echo ""
            echo "### README (excerpt)"
            echo "$README"
            echo ""
            echo "### Recent Activity"
            echo "Open issues: $OPEN_ISSUES_COUNT"
            echo "Recent commits:"
            echo "$RECENT_COMMITS"
            echo ""
            echo "### Recent Product Owner Decisions"
            echo "$PRODUCT_DECISIONS" | python3 -c "
          import json, sys
          items = json.load(sys.stdin)
          if not items:
              print('No recent product-owner issues found.')
          for item in items:
              print(f\"- #{item['number']}: {item['title']} ({item.get('createdAt','')[:10]})\")
          " 2>/dev/null || echo "$PRODUCT_DECISIONS"
            echo "COUNCIL_CTX_EOF"
          } >> "$GITHUB_OUTPUT"

      # ── Acknowledge the council request ─────────────────────────────────
      - name: Post council acknowledgment
        if: |
          steps.resolve.outputs.ref_number != '' &&
          (github.event_name == 'issues' || github.event_name == 'pull_request')
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REF_NUMBER: ${{ steps.resolve.outputs.ref_number }}
          MODE: ${{ steps.resolve.outputs.mode }}
        run: |
          if [ "$MODE" = "role-adjustment" ]; then
            COMMENT="## 🔄 Dynamic Agent Role Adjustment Initiated

          Casey is reviewing current project metrics to rebalance agent roles in real time.

          **Participants**:
          - 🔍 Quinn (QA Engineer)
          - 📋 Morgan (Project Manager)
          - 🧪 Alex (Product Owner)

          Agents will be notified of any role changes in the resulting audit record.

          **Moderator**: Casey (Council Moderator)

          *[Workflow run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})*"
          else
            COMMENT="## 🏛️ Council Discussion Initiated

            The engineering council has been convened to review this topic.

            **Participants**:
            - 🔍 Quinn (QA Engineer) — code quality, security, testing
            - 📋 Morgan (Project Manager) — timeline, priorities, dependencies
            - 🧪 Alex (Product Owner) — user value, product fit, acceptance criteria

            **Moderator**: Casey (Council Moderator)

            Results will be posted shortly. ⏳

            *[Workflow run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})*"
          fi

          if [ "${{ github.event_name }}" = "issues" ]; then
            gh issue comment "$REF_NUMBER" --body "$COMMENT"
          else
            gh pr comment "$REF_NUMBER" --body "$COMMENT"
          fi

      # ══════════════════════════════════════════════════════════════════════
      # Round 1 — Each agent analyses independently
      # ══════════════════════════════════════════════════════════════════════

      - name: Gather role-adjustment metrics
        if: steps.resolve.outputs.mode == 'role-adjustment'
        id: role-data
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh api "repos/${{ github.repository }}/actions/runs?per_page=100" > /tmp/council-runs-page1.json
          gh api "repos/${{ github.repository }}/actions/runs?per_page=100&page=2" > /tmp/council-runs-page2.json
          gh api "repos/${{ github.repository }}/actions/runs?status=in_progress&per_page=50" > /tmp/council-active-runs.json

          python3 - <<'PYEOF'
          import json

          def load(path):
              try:
                  with open(path) as fh:
                      return json.load(fh).get("workflow_runs", [])
              except Exception:
                  return []

          runs = load("/tmp/council-runs-page1.json") + load("/tmp/council-runs-page2.json")
          seen_ids = {run.get("id") for run in runs}
          for run in load("/tmp/council-active-runs.json"):
              if run.get("id") not in seen_ids:
                  runs.append(run)

          with open("/tmp/council-runs.json", "w", encoding="utf-8") as fh:
              json.dump({"workflow_runs": runs}, fh)
          PYEOF

      - name: Build role-adjustment report
        if: steps.resolve.outputs.mode == 'role-adjustment'
        id: role-adjustment
        env:
          CURRENT_DATE: ${{ steps.resolve.outputs.current_date }}
          WORKFLOW_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          TOPIC: ${{ steps.resolve.outputs.topic }}
          BASE_CONTEXT: ${{ steps.resolve.outputs.base_context }}
        run: |
          printf '[]\n' > /tmp/council-role-issues.json
          {
            echo "$TOPIC"
            echo
            echo "$BASE_CONTEXT"
          } > /tmp/council-role-context.txt

          python3 .github/scripts/task_assignment.py \
            --runs /tmp/council-runs.json \
            --issues /tmp/council-role-issues.json \
            --output role-adjustments \
            --context-text "$(cat /tmp/council-role-context.txt)" \
            > /tmp/council-role-adjustment.md

          {
            echo "report<<ROLE_ADJUSTMENT_EOF"
            cat /tmp/council-role-adjustment.md
            echo "ROLE_ADJUSTMENT_EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Quinn — QA perspective
        if: steps.resolve.outputs.mode != 'role-adjustment'
        id: qa-perspective
        uses: ./.github/actions/call-copilot-model
        with:
          model: ${{ env.AGENT_MODEL }}
          max-tokens: ${{ env.AGENT_MAX_TOKENS }}
          temperature: ${{ env.AGENT_TEMPERATURE }}
          token: ${{ secrets.COPILOT_GITHUB_TOKEN || secrets.GITHUB_TOKEN }}
          system-prompt: |
            You are Quinn, a senior QA Engineer. You are participating in an
            engineering council discussion. Provide your honest professional
            perspective focused on quality, security, and testability.

            Configured skill set for this run (repository variable QA_AGENT_SKILLS):
            ${{ env.QA_AGENT_SKILLS }}
            Keep your analysis aligned to these configured skills.

            Be concise (max 300 words). Structure your response as:

            ### Quinn's Perspective (QA Engineer)
            **Quality Concerns**: [what risks do you see?]
            **Security Considerations**: [any security implications?]
            **Testability**: [how would you validate this?]
            **QA Verdict**: [SUPPORT / CONDITIONALLY SUPPORT / OPPOSE] — one sentence rationale
          user-prompt: |
            Council topic: ${{ steps.resolve.outputs.topic }}

            New real-time messages available: none yet.

            ${{ steps.resolve.outputs.base_context }}

      - name: Notify participants — Quinn message
        if: steps.resolve.outputs.mode != 'role-adjustment'
        id: notify-qa
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REF_NUMBER: ${{ steps.resolve.outputs.ref_number }}
        run: |
          if [ -n "$REF_NUMBER" ] && [ "${{ github.event_name }}" = "issues" ]; then
            gh issue comment "$REF_NUMBER" --body "🔔 New council message from **Quinn (QA Engineer)** has been logged and shared with Morgan and Alex."
          elif [ -n "$REF_NUMBER" ] && [ "${{ github.event_name }}" = "pull_request" ]; then
            gh pr comment "$REF_NUMBER" --body "🔔 New council message from **Quinn (QA Engineer)** has been logged and shared with Morgan and Alex."
          fi

      - name: Morgan — PM perspective
        if: steps.resolve.outputs.mode != 'role-adjustment'
        id: pm-perspective
        uses: ./.github/actions/call-copilot-model
        with:
          model: ${{ env.AGENT_MODEL }}
          max-tokens: ${{ env.AGENT_MAX_TOKENS }}
          temperature: ${{ env.AGENT_TEMPERATURE }}
          token: ${{ secrets.COPILOT_GITHUB_TOKEN || secrets.GITHUB_TOKEN }}
          system-prompt: |
            You are Morgan, an experienced Project Manager. You are participating
            in an engineering council discussion. Provide your honest professional
            perspective focused on timelines, resources, and project impact.

            Configured skill set for this run (repository variable PM_AGENT_SKILLS):
            ${{ env.PM_AGENT_SKILLS }}
            Keep your analysis aligned to these configured skills.

            Be concise (max 300 words). Structure your response as:

            ### Morgan's Perspective (Project Manager)
            **Timeline Impact**: [how does this affect the roadmap?]
            **Resource Requirements**: [what effort/team is needed?]
            **Dependencies & Risks**: [what could go wrong?]
            **PM Verdict**: [SUPPORT / CONDITIONALLY SUPPORT / OPPOSE] — one sentence rationale
          user-prompt: |
            Council topic: ${{ steps.resolve.outputs.topic }}

            New real-time messages available from council participants:
            ${{ steps.qa-perspective.outputs.response }}

            ${{ steps.resolve.outputs.base_context }}

      - name: Notify participants — Morgan message
        if: steps.resolve.outputs.mode != 'role-adjustment'
        id: notify-pm
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REF_NUMBER: ${{ steps.resolve.outputs.ref_number }}
        run: |
          if [ -n "$REF_NUMBER" ] && [ "${{ github.event_name }}" = "issues" ]; then
            gh issue comment "$REF_NUMBER" --body "🔔 New council message from **Morgan (Project Manager)** has been logged and shared with Alex for the next response."
          elif [ -n "$REF_NUMBER" ] && [ "${{ github.event_name }}" = "pull_request" ]; then
            gh pr comment "$REF_NUMBER" --body "🔔 New council message from **Morgan (Project Manager)** has been logged and shared with Alex for the next response."
          fi

      - name: Alex — PO perspective
        if: steps.resolve.outputs.mode != 'role-adjustment'
        id: po-perspective
        uses: ./.github/actions/call-copilot-model
        with:
          model: ${{ env.AGENT_MODEL }}
          max-tokens: ${{ env.AGENT_MAX_TOKENS }}
          temperature: ${{ env.AGENT_TEMPERATURE }}
          token: ${{ secrets.COPILOT_GITHUB_TOKEN || secrets.GITHUB_TOKEN }}
          system-prompt: |
            You are Alex, a Product Owner. You are participating in an engineering
            council discussion. Provide your honest professional perspective
            focused on user value, product strategy, and business impact.

            Configured skill set for this run (repository variable PO_AGENT_SKILLS):
            ${{ env.PO_AGENT_SKILLS }}
            Keep your analysis aligned to these configured skills.

            Be concise (max 300 words). Structure your response as:

            ### Alex's Perspective (Product Owner)
            **User Value**: [how does this benefit users?]
            **Product Strategy Fit**: [does this align with product direction?]
            **Acceptance Criteria**: [how do we know when it's done?]
            **PO Verdict**: [SUPPORT / CONDITIONALLY SUPPORT / OPPOSE] — one sentence rationale
          user-prompt: |
            Council topic: ${{ steps.resolve.outputs.topic }}

            New real-time messages available from council participants:
            ${{ steps.qa-perspective.outputs.response }}

            ---

            ${{ steps.pm-perspective.outputs.response }}

            ${{ steps.resolve.outputs.base_context }}

      - name: Notify participants — Alex message
        if: steps.resolve.outputs.mode != 'role-adjustment'
        id: notify-po
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REF_NUMBER: ${{ steps.resolve.outputs.ref_number }}
        run: |
          if [ -n "$REF_NUMBER" ] && [ "${{ github.event_name }}" = "issues" ]; then
            gh issue comment "$REF_NUMBER" --body "🔔 New council message from **Alex (Product Owner)** has been logged and added to the council record for moderator synthesis."
          elif [ -n "$REF_NUMBER" ] && [ "${{ github.event_name }}" = "pull_request" ]; then
            gh pr comment "$REF_NUMBER" --body "🔔 New council message from **Alex (Product Owner)** has been logged and added to the council record for moderator synthesis."
          fi

      # ══════════════════════════════════════════════════════════════════════
      # Round 2 — Council Moderator synthesises
      # ══════════════════════════════════════════════════════════════════════

      - name: Casey — Council Moderator synthesis
        if: steps.resolve.outputs.mode != 'role-adjustment'
        id: moderator
        uses: ./.github/actions/call-copilot-model
        with:
          model: ${{ env.COUNCIL_MODEL }}
          max-tokens: 3000
          temperature: '0.5'
          token: ${{ secrets.COPILOT_GITHUB_TOKEN || secrets.GITHUB_TOKEN }}
          system-prompt: |
            You are Casey, the Council Moderator for an autonomous engineering
            team. Your role is to synthesise the perspectives of the QA Engineer,
            Project Manager, and Product Owner into a clear, actionable council
            decision.

            Configured skill set for this run (repository variable COUNCIL_AGENT_SKILLS):
            ${{ env.COUNCIL_AGENT_SKILLS }}
            Keep your analysis aligned to these configured skills.

            Be fair, impartial, and evidence-driven. Acknowledge disagreements
            explicitly. Your decision must be concrete and actionable.

            Format your response EXACTLY as:

            # 🏛️ Council Decision

            ## Topic
            State the topic being decided.

            ## Council Perspectives Summary

            | Agent | Verdict | Key Point |
            |-------|---------|-----------|
            | 🔍 Quinn (QA) | SUPPORT/CONDITIONAL/OPPOSE | one sentence |
            | 📋 Morgan (PM) | SUPPORT/CONDITIONAL/OPPOSE | one sentence |
            | 🧪 Alex (PO) | SUPPORT/CONDITIONAL/OPPOSE | one sentence |

            ## Points of Agreement
            What all agents agree on.

            ## Points of Tension
            Where agents disagree and why. If no tensions, write "All agents are aligned."

            ## Council Decision
            **Outcome**: [APPROVED ✅ | APPROVED WITH CONDITIONS ⚠️ | DEFERRED 🔄 | REJECTED ❌]

            Rationale: Two to three sentences explaining the decision.

            ## Action Items
            Numbered list of concrete next steps with suggested owners:
            1. **[Owner]** Action description
            2. ...

            ## Conditions (if applicable)
            Any conditions that must be met for approval. Write "None" if unconditional.
          user-prompt: |
            Please synthesise the following council perspectives on the topic:

            **Topic**: ${{ steps.resolve.outputs.topic }}

            ---

            ${{ steps.qa-perspective.outputs.response }}

            ---

            ${{ steps.pm-perspective.outputs.response }}

            ---

            ${{ steps.po-perspective.outputs.response }}

      # ── Post council decision ────────────────────────────────────────────
      - name: Compose full council record
        if: steps.resolve.outputs.mode != 'role-adjustment'
        id: compose
        env:
          QA_RESP: ${{ steps.qa-perspective.outputs.response }}
          PM_RESP: ${{ steps.pm-perspective.outputs.response }}
          PO_RESP: ${{ steps.po-perspective.outputs.response }}
          MODERATOR_RESP: ${{ steps.moderator.outputs.response }}
          TOPIC: ${{ steps.resolve.outputs.topic }}
          DATE: ${{ steps.resolve.outputs.current_date }}
        run: |
          FULL_RECORD="$MODERATOR_RESP

          ---

          ## Council Transcript

          <details>
          <summary>View individual agent perspectives</summary>

          $QA_RESP

          ---

          $PM_RESP

          ---

          $PO_RESP

          </details>

          ---
          ## Real-Time Message Log

          ### $DATE — Quinn (QA Engineer)

          $QA_RESP

          ---

          ### $DATE — Morgan (Project Manager)

          $PM_RESP

          ---

          ### $DATE — Alex (Product Owner)

          $PO_RESP

          ---
          *🤖 Council facilitated by Casey (Council Moderator) · Date: $DATE · [Workflow run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})*"

          {
            echo "full_record<<COUNCIL_RECORD_EOF"
            echo "$FULL_RECORD"
            echo "COUNCIL_RECORD_EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Select final council record
        id: final-record
        env:
          MODE: ${{ steps.resolve.outputs.mode }}
          TOPIC: ${{ steps.resolve.outputs.topic }}
          DISCUSSION_RECORD: ${{ steps.compose.outputs.full_record }}
          ROLE_RECORD: ${{ steps.role-adjustment.outputs.report }}
        run: |
          if [ "$MODE" = "role-adjustment" ]; then
            TITLE="🔄 Dynamic Agent Role Adjustment: $TOPIC"
            BODY="$ROLE_RECORD"
          else
            TITLE="🏛️ Council Decision: $TOPIC"
            BODY="$DISCUSSION_RECORD"
          fi

          echo "title=$TITLE" >> "$GITHUB_OUTPUT"
          {
            echo "body<<COUNCIL_BODY_EOF"
            echo "$BODY"
            echo "COUNCIL_BODY_EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Post council decision
        id: post-decision
        uses: ./.github/actions/post-council-results
        with:
          title: ${{ steps.final-record.outputs.title }}
          body: ${{ steps.final-record.outputs.body }}
          category: ${{ vars.COUNCIL_DISCUSSION_CATEGORY || 'Team Decisions' }}
          labels: 'council-decision'
          token: ${{ secrets.GITHUB_TOKEN }}

      # ── Link decision back to the originating issue/PR ───────────────────
      - name: Link decision to originating issue/PR
        if: steps.resolve.outputs.ref_number != ''
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REF_NUMBER: ${{ steps.resolve.outputs.ref_number }}
          DECISION_URL: ${{ steps.post-decision.outputs.url }}
          DECISION_TYPE: ${{ steps.post-decision.outputs.type }}
          MODE: ${{ steps.resolve.outputs.mode }}
        run: |
          if [ "$MODE" = "role-adjustment" ]; then
            COMMENT="## 🔄 Dynamic Agent Role Adjustment Posted

            Casey has published a real-time role-adjustment record based on the latest workflow metrics.

            **View full report**: $DECISION_URL
            **Posted as**: $DECISION_TYPE

            This report includes the role allocation summary, immediate notifications, and the audit log entry.

            ---
            *🤖 Automated council role adjustment · [Workflow run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})*"
          else
            COMMENT="## 🏛️ Council Decision Posted

            The engineering council has reached a decision.

            **View full decision**: $DECISION_URL
            **Posted as**: $DECISION_TYPE

            $(echo '${{ steps.moderator.outputs.response }}' | head -20)

            ---
            *🤖 Automated council decision · [Workflow run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})*"
          fi

          if [ -n "$REF_NUMBER" ] && { [ "${{ github.event_name }}" = "issues" ] || [ "${{ github.event_name }}" = "workflow_call" ]; }; then
            gh issue comment "$REF_NUMBER" --body "$COMMENT" 2>/dev/null || true
          elif [ -n "$REF_NUMBER" ] && [ "${{ github.event_name }}" = "pull_request" ]; then
            gh pr comment "$REF_NUMBER" --body "$COMMENT" 2>/dev/null || true
          fi

      - name: Reply to originating discussion comment
        if: |
          github.event_name == 'discussion_comment' &&
          steps.resolve.outputs.discussion_reply_id != ''
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DISCUSSION_ID: ${{ steps.resolve.outputs.discussion_reply_id }}
          REPLY_TO_ID: ${{ steps.resolve.outputs.discussion_reply_to_id }}
          DECISION_URL: ${{ steps.post-decision.outputs.url }}
          DECISION_TYPE: ${{ steps.post-decision.outputs.type }}
          MODERATOR_RESPONSE: ${{ steps.moderator.outputs.response }}
        run: |
          OUTCOME=$(printf '%s\n' "$MODERATOR_RESPONSE" | sed -nE 's/^\*\*Outcome\*\*:[[:space:]]*//p' | head -1)
          if [ -z "$OUTCOME" ]; then
            OUTCOME="Council response generated; see the linked decision artifact for the full record."
          fi
          BODY=$(printf '%s\n\n%s\n\n**Outcome**: %s\n\n**Full council record**: %s\n**Posted as**: %s\n\n%s\n\n---\n%s' \
            '## 🏛️ Council Reply' \
            'Casey convened the council in response to this comment.' \
            "$OUTCOME" \
            "$DECISION_URL" \
            "$DECISION_TYPE" \
            'The full record includes the QA, PM, PO, and moderator perspectives. Keeping this reply short so the discussion thread stays readable.' \
            '*🤖 Automated council reply · [Workflow run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})*')

          if [ -n "$REPLY_TO_ID" ]; then
            gh api graphql \
              -f query='mutation($discussionId:ID!,$replyToId:ID!,$body:String!){addDiscussionComment(input:{discussionId:$discussionId,replyToId:$replyToId,body:$body}){comment{url}}}' \
              -f discussionId="$DISCUSSION_ID" \
              -f replyToId="$REPLY_TO_ID" \
              -f body="$BODY"
          else
            gh api graphql \
              -f query='mutation($discussionId:ID!,$body:String!){addDiscussionComment(input:{discussionId:$discussionId,body:$body}){comment{url}}}' \
              -f discussionId="$DISCUSSION_ID" \
              -f body="$BODY"
          fi
